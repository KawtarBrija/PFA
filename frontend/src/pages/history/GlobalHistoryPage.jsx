import HistoryPage from './HistoryPage';

export default function GlobalHistoryPage() {
  return <HistoryPage title="History" subtitle="Full terminal activity across all agents, searchable and filterable." />;
}

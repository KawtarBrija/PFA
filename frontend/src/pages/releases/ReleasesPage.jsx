import ReleasesTable from './ReleasesTable';

export default function ReleasesPage() {
  return (
    <ReleasesTable
      title="Releases"
      subtitle="Read-only view of every container release, searchable and filterable."
    />
  );
}

import HistoryPage from '../history/HistoryPage';

export default function ReviewHistoryPage() {
  return <HistoryPage title="Review History" subtitle="Operations you have recorded, searchable and filterable." />;
}
